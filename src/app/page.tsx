
import { Button } from "./components/button";
import { IconButton } from "./components/icon-button";

import "./globals.css";
import { ArrowRight, Mail, Radio, User, Copy } from 'lucide-react' // npm install lucide-react


export default function Home() {
  return (
    <main>
      <Button>
         Enviar
        <ArrowRight/>
      </Button>

      <IconButton>
        
             </IconButton>
    </main>
  );
}
